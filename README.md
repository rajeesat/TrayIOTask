Please follow the steps to run the project.

1) If you haven’t already installed Node.js, download it from its official website(https://nodejs.org/en/download/) and export node.js PATH to your Local PATH(for Mac user Bashrc file for windows set environment variable).

2) Clone the github project from here[] and go to that project folder location.

3) Install Selenium webdriver.

4)You have to download the chrome driver[http://chromedriver.chromium.org/downloads] for a specific selenium webdriver and add that to your local Path.ChromeDriver executable needs to be available in the path.

5)Navigate to src folder to run the test.

6)Type node CreateAndDeleteWFTest.js from folder src through terminal(for Mac user)

You can see the chrome browser have opened and begin to test perform.
